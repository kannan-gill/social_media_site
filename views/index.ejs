<%-include ("partials/header") %>


    <div class="container">
        <header class="jumbotron">
            <div class="container">
            <h1>Welcome to Brozone!</h1>
            <p>Not men, not ladies, just BROS!!</p>
            <p>
                <a class="btn btn-primary btn-lg" href="/campgrounds/new">Whatsup? Tell the world!!</a>
            </p>
            </div>
            </header>

    <div class="row text-center" style="display:flex;flex-wrap:wrap;">
            <%for(var i=campgrounds.length-1;i>=0;i--){%>
            <div class="col-md-12 col-sm-6">
                <div class="thumbnail">
                    <img src="<%=campgrounds[i].image%>">
                    <div class="caption">
                        <h4><%= campgrounds[i].name%></h4>    
                    </div>
                    <p>
                        <a href="/campgrounds/<%=campgrounds[i]._id%>" class="btn btn-primary">Kno what the bro says</a>
                    </p>
                </div>
            </div>
            <%};%>
        </div>
    </div>
    
<%-include ("partials/footer") %>

<!--campgrounds.forEach(function(campground){-->